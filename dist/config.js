// Runtime configuration. This file is intentionally separate from the compiled app.
// Leave apiBaseUrl empty when the frontend is served from the same origin as the backend.
window.__RETROBOLT_ADMIN_CONFIG__ = {
  apiBaseUrl: "",
};
